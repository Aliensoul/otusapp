{{- if index .Values "prometheus-operator" "serviceMonitor" "enabled" }}
apiVersion: monitoring.coreos.com/v1
kind: ServiceMonitor
metadata:
  name: {{ template "homework.fullname" . }}-orders
  labels:
    app: {{ template "homework.fullname" . }}-orders
  {{- include "homework.labels" . | nindent 4 }}
spec:
  jobLabel: {{ include "homework.fullname" . }}-orders
  namespaceSelector:
    matchNames:
      - "{{ $.Release.Namespace }}"
  selector:
    matchLabels:
      app: {{ template "homework.fullname" . }}-orders
  {{- include "homework.selectorLabels" . | nindent 6 }}
  endpoints:
    - interval: 15s
      port: orders-http
      path: /metrics
  {{- end }}