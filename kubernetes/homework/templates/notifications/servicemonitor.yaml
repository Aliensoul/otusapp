{{- if index .Values "prometheus-operator" "serviceMonitor" "enabled" }}
apiVersion: monitoring.coreos.com/v1
kind: ServiceMonitor
metadata:
  name: {{ template "homework.fullname" . }}-notifications
  labels:
    app: {{ template "homework.fullname" . }}-notifications
  {{- include "homework.labels" . | nindent 4 }}
spec:
  jobLabel: {{ include "homework.fullname" . }}-notifications
  namespaceSelector:
    matchNames:
      - "{{ $.Release.Namespace }}"
  selector:
    matchLabels:
      app: {{ template "homework.fullname" . }}-notifications
  {{- include "homework.selectorLabels" . | nindent 6 }}
  endpoints:
    - interval: 15s
      port: notif-http
      path: /metrics
  {{- end }}