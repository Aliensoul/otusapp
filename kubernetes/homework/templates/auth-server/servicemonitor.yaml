{{- if index .Values "prometheus-operator" "serviceMonitor" "enabled" }}
apiVersion: monitoring.coreos.com/v1
kind: ServiceMonitor
metadata:
  name: {{ include "homework.fullname" . }}-authserver
  labels:
  {{- include "homework.labels" . | nindent 4 }}-authserver
spec:
  jobLabel: {{ include "homework.fullname" . }}-authserver
  namespaceSelector:
    matchNames:
      - "{{ $.Release.Namespace }}"
  selector:
    matchLabels:
  {{- include "homework.selectorLabels" . | nindent 6 }}-authserver
  endpoints:
    - interval: 15s
      port: http
      path: /metrics
  {{- end }}