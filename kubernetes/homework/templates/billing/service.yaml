apiVersion: v1
kind: Service
metadata:
  name: {{ include "homework.fullname" . }}-billing
  labels:
    app: {{ include "homework.fullname" . }}-billing
  {{- include "homework.labels" . | nindent 4 }}
spec:
  type: {{ .Values.billing.service.type }}
  ports:
    - port: {{ .Values.billing.service.port }}
      targetPort: {{ .Values.billing.service.portName }}
      protocol: TCP
      name: {{ .Values.billing.service.portName}}
  selector:
    app: {{ include "homework.fullname" . }}-billing
  {{- include "homework.selectorLabels" . | nindent 4 }}