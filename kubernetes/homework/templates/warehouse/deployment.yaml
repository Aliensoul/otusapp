apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ include "homework.fullname" . }}-warehouse
  labels:
    app: {{ include "homework.fullname" . }}-warehouse
    {{- include "homework.labels" . | nindent 4 }}
spec:
  replicas: {{ .Values.warehouse.replicaCount }}
  selector:
    matchLabels:
      app: {{ include "homework.fullname" . }}-warehouse
      {{- include "homework.selectorLabels" . | nindent 6 }}
  template:
    metadata:
      annotations:
        checksum/config: {{ include (print $.Template.BasePath "/config.yaml") . | sha256sum }}
      labels:
        app: {{ include "homework.fullname" . }}-warehouse
        {{- include "homework.selectorLabels" . | nindent 8 }}
    spec:
      containers:
        - name: {{ .Values.warehouse.name }}
          image: "{{ .Values.warehouse.image.repository }}:{{.Chart.AppVersion }}"
          ports:
            - name: {{.Values.warehouse.service.portName}}
              containerPort: {{.Values.warehouse.service.port}}
              protocol: TCP
          livenessProbe:
            httpGet:
              path: /health/liveness
              port: {{.Values.warehouse.service.portName}}
            initialDelaySeconds: 20
          readinessProbe:
            httpGet:
              path: /health/readiness
              port: {{.Values.warehouse.service.portName}}
            initialDelaySeconds: 20
          env:
            - name: DATABASE_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: warehouse-secret
                  key: DATABASE_PASSWORD
            - name: DATABASE_HOST
              valueFrom:
                configMapKeyRef:
                  key: DATABASE_HOST
                  name: warehouse-config
            - name: DATABASE_DATABASE
              valueFrom:
                configMapKeyRef:
                  key: DATABASE_DATABASE
                  name: warehouse-config
            - name: DATABASE_USERNAME
              valueFrom:
                configMapKeyRef:
                  key: DATABASE_USERNAME
                  name: warehouse-config
            - name: KAFKA_SERVERS
              valueFrom:
                configMapKeyRef:
                  name: common-config
                  key: KAFKA_SERVERS