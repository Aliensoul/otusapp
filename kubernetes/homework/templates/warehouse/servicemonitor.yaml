{{- if index .Values "prometheus-operator" "serviceMonitor" "enabled" }}
apiVersion: monitoring.coreos.com/v1
kind: ServiceMonitor
metadata:
  name: {{ include "homework.fullname" . }}-warehouse
  labels:
  {{- include "homework.labels" . | nindent 4 }}-warehouse
spec:
  jobLabel: {{ include "homework.fullname" . }}-warehouse
  namespaceSelector:
    matchNames:
      - "{{ $.Release.Namespace }}"
  selector:
    matchLabels:
  {{- include "homework.selectorLabels" . | nindent 6 }}-warehouse
  endpoints:
    - interval: 15s
      port: http
      path: /metrics
  {{- end }}