{{- if .Values.warehouse.ingress.enabled -}}
  {{- $fullName := include "homework.fullname" . -}}
  {{- $svcPort := .Values.warehouse.service.port -}}
apiVersion: networking.k8s.io/v1beta1
kind: Ingress
metadata:
  name: {{ $fullName }}-warehouse
  labels:
    app: {{ include "homework.fullname" . }}-warehouse
      {{- include "homework.labels" . | nindent 4 }}
  {{- with .Values.warehouse.ingress.annotations }}
  annotations:
  {{- toYaml . | nindent 4 }}
  {{- end }}
spec:
  rules:
    {{- range .Values.warehouse.ingress.hosts }}
    - host: {{ .host | quote }}
      http:
        paths:
          {{- range .paths }}
          - path: {{ . }}
            backend:
              serviceName: {{ $fullName }}-warehouse
              servicePort: {{ $svcPort }}
        {{- end }}
  {{- end }}
  {{- end }}